{
	"devDependencies": {
		"@biomejs/biome": "2.3.14",
		"lefthook": "^2.1.0"
	},
	"scripts": {
		"lint:unstaged:fix": "git diff --name-only --diff-filter=ACM | xargs -r biome check --write --unsafe"
	}
}
